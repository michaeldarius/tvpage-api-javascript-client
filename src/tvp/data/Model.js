define([
	"backbone"
],
	function(Backbone) {
    /**
     * Abstract model
     * 
     * @type @exp;Backbone@pro;Model@call;extend
     */
		var TVPModel = Backbone.Model.extend({
		});

		return TVPModel;
	});
